const userData = require("./users");
const billsData = require("./bills");
const budgetData = require("./budget");

module.exports = {
    users: userData,
    bills : billsData,
    budget : budgetData
};
